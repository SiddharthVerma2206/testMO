# testMO rate limit zone — /etc/nginx/conf.d/testmo-ratelimit.conf
#
# limit_req_zone is only valid in the http block, which is why this lives in
# conf.d rather than inside the server block in sites-available/testmo.
limit_req_zone $binary_remote_addr zone=testmo:10m rate=10r/s;
