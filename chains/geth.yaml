chain_type: "ethereum-geth"
client_name: "geth"

# Requires --metrics --metrics.addr 127.0.0.1 --metrics.port 6060.
# Scraped by Prometheus as job 'rpc_client'; the geth_* / chain_* / rpc_* /
# discover_* expressions at the bottom read it.
metrics_port: 6060
metrics_path: "/debug/metrics/prometheus"

rpc:
  url: "http://127.0.0.1:8545"
  timeout: "3s"
  interval: "5s"

local_calls:
  - name: "probe_syncing"
    method: "eth_syncing"
    params: []
    extract: {type: "bool"}

  - name: "probe_network_id"
    method: "net_version"
    extract: {type: "number"}

  - name: "probe_chain_id"
    method: "eth_chainId"
    extract: {type: "hex"}

metric_map:
  testMO_rpc_up: 'probe_call_up{call="probe_syncing"}'
  testMO_syncing: "probe_syncing"
  testMO_peers: "p2p_peers"
  testMO_block_height: "chain_head_block"
  testMO_blocks_per_minute: "deriv(chain_head_block[5m]) * 60"

  testMO_rpc_latency_ms: 'probe_call_duration_ms{call="probe_syncing"}'
  testMO_rpc_latency_p50_ms: 'quantile_over_time(0.50, probe_call_duration_ms{call="probe_syncing"}[30m])'
  testMO_rpc_latency_p95_ms: 'quantile_over_time(0.95, probe_call_duration_ms{call="probe_syncing"}[30m])'
  testMO_rpc_latency_p99_ms: 'quantile_over_time(0.99, probe_call_duration_ms{call="probe_syncing"}[30m])'

  testMO_chain_id: "probe_chain_id"
  testMO_network_id: "probe_network_id"

  testMO_metrics_up: 'up{job="rpc_client"}'

  testMO_rpc_requests_per_min: "rate(rpc_requests[5m]) * 60"
  testMO_rpc_success_pct: "100 * rate(rpc_success[5m]) / rate(rpc_requests[5m])"

  testMO_discover_ingress_bps: "rate(discover_ingress[5m])"
  testMO_discover_egress_bps: "rate(discover_egress[5m])"
  testMO_rpc_failures_per_min: "rate(rpc_failure[5m]) * 60"